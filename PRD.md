# AGS 数据台 · 产品需求文档（PRD）

| 项目 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 编写日期 | 2026-08-30 |
| 状态 | 需求已确认，布局草图已评审待反馈 |
| 适用版本 | 第一版（MVP） |
| 项目目录 | `C:\Users\sky\Desktop\数据APP` |

> **本文档的标记约定**
> - 【已确认】用户明确拍板，开发必须遵守，不得自行变更
> - 【假设】我替用户做的默认决定，用户已认可；如需改动请显式提出
> - 【待定】尚未确认，开发前必须解决

---

## 0. 一句话说明

给养好氧颗粒污泥（AGS）的研究生做一个**只在本机跑**的数据管理工具：把每天和全周期测的吸光度自动换算成浓度，存起来，能查、能画图、能算去除率。不联网、不登录、不丢数据。

---

## 1. 项目背景与目标

### 1.1 背景

用户是环境工程方向研究生，运行 **AOA（厌氧-好氧-缺氧）工艺的好氧颗粒污泥系统**，同时养多个反应器（罐），数量可变。

日常需要每天测定的**五个基础指标**：

| 指标 | 计量方式 | 说明 |
|---|---|---|
| 氨氮 NH₄⁺-N | 吸光度换算 | 【已确认】 |
| 硝态氮 NO₃⁻-N | 吸光度换算 | 【已确认】 |
| 亚硝态氮 NO₂⁻-N | 吸光度换算 | 【已确认】 |
| 总磷 TP | 吸光度换算 | 【已确认】 |
| COD | 仪器直读 | 【已确认】不参与吸光度换算 |

另有不定期测定的扩展指标：氯离子、EPS、粒径、MLSS、MLVSS、SVI、高氯酸盐等。

### 1.2 要解决的问题

1. **手算太累**：吸光度 → 浓度要套标准曲线还要乘稀释倍数，每天几十个数据
2. **数据散落**：现在大概躺在 Excel 里，查历史、做对比、画图都很费劲
3. **全周期数据量爆炸**：一周期几小时、每罐每半小时一次、五个指标、多个罐，人工整理不现实
4. **口径容易乱**：重做了标准曲线，以前的数据该按新曲线还是旧曲线算？必须可追溯

### 1.3 成功标准（用户可自行验证）

- 录入当天所有罐的数据不超过 3 分钟
- 全周期数据能从 Excel 整块粘进来，不用一格一格敲
- 关闭浏览器、重启电脑后，数据一条不少
- 重做标准曲线后，历史数据算出来的浓度**一个数字都不变**
- 三罐三十天的氨氮趋势图，点几下就能出，能导出图片直接用进汇报

---

## 2. 用户与使用场景

### 2.1 用户画像

单人使用，编程小白。电脑上做主要的数据整理和分析，手机上做快速录入和查看。
**所有用词、报错提示、按钮文案都必须说人话，不出现技术黑话。**

### 2.2 场景一：日常测量（每天一次）

选日期 → 填进水 → 逐罐填水样吸光度 → 软件自动出浓度 → 保存。

### 2.3 场景二：全周期测量（每隔一段时间一次）

一周期持续几小时，每罐每半小时取一次出水，测五个指标。
新建一个"周期实验"，从 Excel 复制整块数据粘贴进来，自动换算，看周期内的变化曲线和速率。

---

## 3. 术语表

| 术语 | 含义 |
|---|---|
| 反应器 / 罐 | 一个独立的 AGS 反应装置，编号如 R1、R2、R3 |
| 指标 | 被测的物质项，如氨氮、COD、粒径 |
| 水样吸光度 | 样品在分光光度计上读到的值 |
| 空白吸光度 | 空白对照读到的值，同批次样品通常共用一个数 |
| 稀释倍数 | 样品被稀释的倍数，如 ×10 |
| 标准曲线 / 标曲 | 用已知浓度标液拟合出的 吸光度 = k × 浓度 + b |
| 当前生效标曲 | 某个指标在某段时间内正在使用的那条曲线 |
| 日常数据 | 每天测一次的常规数据 |
| 全周期数据 | 一个周期内按固定间隔密集采样的数据 |
| 阶段 | AOA 工艺的厌氧段 / 好氧段 / 缺氧段 |

---

## 4. 核心数据模型

存储采用**长表**结构（一条记录 = 一次"某天某罐某指标"的测量），展示时再拼成表格。
这样加指标、加罐都不需要改结构。

### 4.1 实体清单

| 实体 | 说明 |
|---|---|
| Reactor 反应器 | 罐的档案 |
| Indicator 指标 | 指标的定义与计量方式 |
| CalibrationCurve 标准曲线 | 某指标某段时间内生效的标曲 |
| CycleRun 周期实验 | 一次全周期测量的元信息 |
| Measurement 测量值 | 一次具体测量的输入与结果（核心表） |
| Influent 进水 | 进水浓度 |
| DailyDefault 当次默认值 | 本次录入共用的空白与稀释倍数 |
| CustomRecord 自定义指标记录 | 不定期测的扩展指标（氯离子、EPS 等） |

### 4.2 Reactor 反应器

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 主键 | |
| code | 文本 | 编号，如 R1，全局唯一 |
| name | 文本 | 显示名，可空，默认同编号 |
| note | 文本 | 备注，如"高氯酸盐 5 mg/L 组" |
| active | 布尔 | 启用状态。停用后不出现在录入界面，历史数据保留 |
| sortOrder | 数字 | 显示顺序 |
| createdAt | 日期 | |

### 4.3 Indicator 指标

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 主键 | |
| name | 文本 | 如"氨氮"、"亚硝态氮" |
| category | 枚举 | basic 基础指标 / custom 自定义 |
| method | 枚举 | absorbance 吸光度换算 / direct 直读 |
| unit | 文本 | 默认 mg/L |
| defaultDilution | 数字 | 默认稀释倍数，默认 1 |
| refLow / refHigh | 数字 | 参考范围，可空，用于异常提示 |
| active | 布尔 | |
| sortOrder | 数字 | |

**内置指标初始化**：氨氮、硝态氮、亚硝态氮、总P 为 `absorbance`；COD 为 `direct`。
四个 absorbance 指标**不允许**改成 direct，COD 不允许改成 absorbance（如需改动须在设置里显式解锁）。

### 4.4 CalibrationCurve 标准曲线

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 主键 | |
| indicatorId | 外键 | 所属指标 |
| effectiveFrom | 日期 | 生效起始日 |
| effectiveTo | 日期 | 生效截止日，空 = 至今 |
| k | 数字 | 斜率 |
| b | 数字 | 截距 |
| r2 | 数字 | 决定系数 |
| points | 数组 | 标液点 `[{concentration, absorbance}]`，点数不限 |
| batchNo | 文本 | 试剂批号，选填 |
| note | 文本 | 备注，选填 |
| createdAt | 日期时间 | |

**约束**：同一指标的生效区间不允许重叠。新建曲线时若与已有区间冲突，系统须提示并要求用户选择"覆盖"或"调整生效日"。

### 4.5 CycleRun 周期实验

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 主键 | |
| date | 日期 | |
| name | 文本 | 默认"YYYY-MM-DD 周期"，可改 |
| startTime | 时间 | 起始时刻 |
| intervalMinutes | 数字 | 取样间隔，默认 30 |
| reactorIds | 数组 | 参与的反应器 |
| note | 文本 | |

### 4.6 Measurement 测量值（核心表）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 主键 | |
| scene | 枚举 | daily 日常 / cycle 全周期 |
| date | 日期 | |
| cycleRunId | 外键 | 全周期数据必填，日常为空 |
| time | 时间 | 全周期数据的采样时刻 |
| phase | 枚举 | 厌氧 / 好氧 / 缺氧，选填 |
| reactorId | 外键 | |
| indicatorId | 外键 | |
| inputType | 枚举 | absorbance / direct |
| sampleAbs | 数字 | 水样吸光度，absorbance 时必填 |
| blankAbs | 数字 | 空白吸光度；为空则取当次默认值 |
| dilution | 数字 | 稀释倍数；为空则取当次默认值 |
| value | 数字 | **计算得出的浓度（mg/L）** |
| curveId | 外键 | 本次计算实际使用的标曲 id **（冗余保存，用于追溯）** |
| blankOverridden | 布尔 | 本条是否单独改过空白 |
| dilutionOverridden | 布尔 | 本条是否单独改过稀释倍数 |
| note | 文本 | |

> `curveId` 冗余存储是硬性要求：即使旧标曲被停用、被删除，也能查到某条数据当初是用哪条曲线算的。

### 4.7 Influent 进水

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 主键 | |
| date / cycleRunId | | 归属哪天或哪个周期 |
| mode | 枚举 | **shared 几罐共用 / perReactor 每罐各自**【已确认】 |
| reactorId | 外键 | shared 模式为空 |
| indicatorId | 外键 | |
| value | 数字 | 进水浓度 |
| inputType | 枚举 | 进水同样支持 absorbance 换算与 direct 直读 |

**进水模式说明**
- **shared（常规）**：几罐用同一桶配水，只录一次，所有罐共用
- **perReactor（特例）**：每罐进水不同 —— 用户的高氯酸盐梯度实验，各罐投加浓度不同。此模式下每罐单独录，去除率按罐分别算

### 4.8 CustomRecord 自定义指标记录

不定期测量的扩展指标（氯离子、EPS、粒径、MLSS、MLVSS、SVI、高氯酸盐等）。

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 主键 | |
| date | 日期 | |
| reactorId | 外键 | 可空（有些指标可能不按罐测） |
| indicatorId | 外键 | |
| value | 数字 | |
| note | 文本 | |

第一版只做**录入 + 查询 + 参与相关性分析**，不做专属分析模块。

---

## 5. 计算规则（硬性，不得自行变更）

### 5.1 吸光度 → 浓度

```
A校正 = A水样 − A空白
浓度  = (A校正 − b) / k × 稀释倍数
```

其中 k、b 取自**该测量日期当天该指标正在生效的标准曲线**。

### 5.2 标曲生效规则【已确认，极重要】

- 每个指标独立维护一条"当前生效"标曲，管一段时间
- 新建标曲时可选"从今天起生效"或指定日期
- **生效日之前的历史数据，一律仍按旧曲线计算，绝不改写**
- 标曲只增不改：已停用的曲线保留在历史列表，并显示"仍算着 N 条数据"
- 每条 Measurement 冗余记录 `curveId`，随时可追溯

### 5.3 稀释倍数与空白的继承与覆盖【已确认】

- 录入界面顶部提供"本次共用"的空白吸光度行与默认稀释倍数行，默认值沿用上一次
- 用户可在某一格单独修改，**只影响那一格**（对应记录标记 `blankOverridden` / `dilutionOverridden`，界面上用橙色角标显示）
- 修改默认值不影响任何已保存的历史数据

### 5.4 COD 例外【已确认】

COD 为仪器直读，不设标曲。设置页中 COD 的指标卡片置灰，标注"仪器直读，不用标曲"。
录入时 COD 列直接填浓度，不显示吸光度格。

### 5.5 去除率

```
去除率(%) = (C进水 − C出水) / C进水 × 100
```

- shared 模式：所有罐比同一个进水值
- perReactor 模式：各罐比各自的进水值
- **进水数据缺失时不计算，显示"—"，严禁用 0 或其他值代替**

### 5.6 亚硝积累率（NAR）

```
NAR(%) = NO₂⁻-N / (NO₂⁻-N + NO₃⁻-N) × 100
```

分母为 0 时显示"—"。

### 5.7 时段统计口径

- 平均值、标准差：基于该时段内所有**非空**测量值，空值不计入分母
- 达标率 = 达标天数 / 有数据的天数 × 100%，达标阈值在设置中配置，未配置则不显示该指标

### 5.8 异常值处理原则【已确认】

**第一版只提醒，不自动改数据。**

- 计算值为负 → 照常显示负值，该单元格标黄
- 计算值低于检出限 → 显示"未检出"，不写 0
- 超出指标参考范围 → 标黄
- 任何情况下不自动剔除、不自动修约

---

## 6. 功能需求

### 6.1 模块一 · 数据录入与计算

**目标**：每天一次录完所有罐的所有基础指标，边填边出浓度，一屏搞定。

| # | 功能点 | 验收标准 |
|---|---|---|
| 1.1 | 日期选择 | 顶部可选任意日期，默认今天。切到已有数据的日期时自动带出原值，可修改补录 |
| 1.2 | 进水模式切换 | 顶部可切换"几罐共用 / 每罐各自"。shared 显示 1 行输入，perReactor 每罐 1 行 |
| 1.3 | 当次共用行 | 表格上方两行：本次空白吸光度（按指标）、默认稀释倍数（按指标）。默认沿用上次，可整行改 |
| 1.4 | 出水录入表 | 行 = 反应器，列 = 指标。四个 absorbance 指标各占两列（吸光度输入 / 浓度只读），COD 占一列（直接填） |
| 1.5 | 单格覆盖 | 空白、稀释倍数可在任意格单独修改，只影响该格，界面用橙色角标标记 |
| 1.6 | 实时换算 | 吸光度一改动，同行浓度列立即更新 |
| 1.7 | 视觉区分 | 需填写的格子是白底输入框；自动算出的浓度是无边框绿色字，不可编辑 |
| 1.8 | 异常提示 | 负值、低于检出限、超出参考范围时该格标黄，鼠标悬停说明原因 |
| 1.9 | 保存 | 明确保存动作，保存后写入本地存储。刷新、关闭、重启后数据仍在 |
| 1.10 | 反应器过滤 | 已停用的罐不出现在录入界面，其历史数据完整保留 |

### 6.2 模块二 · 全周期数据统计

**目标**：一次录完一个周期的大量数据，并直接给出周期内的关键结论。

| # | 功能点 | 验收标准 |
|---|---|---|
| 2.1 | 新建周期实验 | 可设日期、名称、起始时间、取样间隔（默认 30 分钟）、参与的反应器 |
| 2.2 | 指标切换 tab | 顶部按指标切换，一次只录一个指标，避免列爆炸【待定：见 OQ-1】 |
| 2.3 | 录入表 | 行 = 时间点，列 = 反应器（吸光度 / 浓度两列） |
| 2.4 | 阶段标记 | 每行可选填厌氧 / 好氧 / 缺氧，选填不强制【假设】 |
| 2.5 | Excel 粘贴 | 从 Excel 框选一整块 Ctrl+V，自动对位填入；列数不匹配时给出明确提示 |
| 2.6 | 空白与稀释 | 同 1.3 / 1.5，支持整列共用与单格覆盖 |
| 2.7 | 周期统计 | 底部四张卡片：起始浓度、最低（或最高）值、降到目标值用时、指定阶段的变化速率 |
| 2.8 | 目标阈值 | "降到 X 用时"中的 X 可在设置中配置 |
| 2.9 | 周期列表 | 可查看、重命名、删除历史周期实验 |

### 6.3 模块三 · 数据查询与整理

**目标**：按任意条件把数据捞出来、排好、导出。

| # | 功能点 | 验收标准 |
|---|---|---|
| 3.1 | 组合筛选 | 日期范围 × 反应器 × 指标 × 数据类型（日常/全周期）× 阶段 |
| 3.2 | 排序 | 按日期、按浓度高低排序 |
| 3.3 | 搜索 | 按备注关键词搜索 |
| 3.4 | 编辑与删除 | 结果表格内可直接改值；支持批量选中删除，删除前二次确认 |
| 3.5 | 导出 | 当前筛选结果一键导出 Excel |
| 3.6 | 追溯 | 每条记录可查看"当时用的是哪条标曲、空白多少、稀释多少" |

> 本模块的页面布局草图尚未绘制【待定：见 OQ-3】

### 6.4 模块四 · 可视化与对比分析

**目标**：几下点出能直接进汇报的图。

| # | 功能点 | 验收标准 |
|---|---|---|
| 4.1 | 筛选面板 | 左侧一栏：数据类型、日期范围、反应器多选、指标多选、对比方式 |
| 4.2 | 日常趋势图 | 横轴日期，一条线一个罐 |
| 4.3 | 周期曲线图 | 横轴时间，多罐叠加 |
| 4.4 | 周期叠周期 | 同一罐不同批次的周期曲线叠在一起对比 |
| 4.5 | 按指标分线 | 同一罐的多个指标放在一张图 |
| 4.6 | 双轴叠加 | 两个量纲差异大的指标共用一张图（如氨氮与 COD） |
| 4.7 | 图例交互 | 点击图例可临时隐藏某条线 |
| 4.8 | 悬停 | 鼠标悬停显示该点的日期、罐号、数值 |
| 4.9 | 框选放大 | 拖拽框选可放大某段时间 |
| 4.10 | 图例统计 | 图例旁显示每条线的均值 |
| 4.11 | 导出 | 图可导出为 PNG；配色与线型适合放进论文和 PPT |

### 6.5 模块五 · 统计与辅助分析

**目标**：给出这个工艺真正关心的几个导出量。

| # | 功能点 | 验收标准 |
|---|---|---|
| 5.1 | 去除率 | 按进水模式正确取进水值；缺失显示"—" |
| 5.2 | 亚硝积累率 NAR | 按 5.6 公式，分母为 0 显示"—" |
| 5.3 | 时段统计 | 选定时间段的平均值、标准差、最大值、最小值、达标率 |
| 5.4 | 相关性分析 | 任选两个指标（含自定义指标）出散点图 + 相关系数 r |
| 5.5 | 分组对比 | 按反应器分组汇总，便于比较高氯酸盐梯度各组差异 |
| 5.6 | 结果导出 | 统计结果导出 Excel |

> 本模块的页面布局草图尚未绘制【待定：见 OQ-3】

### 6.6 模块六 · 系统设置

**目标**：把"会变的东西"集中在这里，改设置不动历史数据。

二级 tab：**反应器 / 标准曲线 / 自定义指标 / 备份与导出**

#### 6.6.1 反应器管理

| # | 功能点 |
|---|---|
| 6.1.1 | 增删改反应器：编号、显示名、备注、排序 |
| 6.1.2 | 启用 / 停用。停用的罐不出现在录入界面，历史数据完整保留 |
| 6.1.3 | 删除反应器时，若存在历史数据须二次确认并告知影响条数 |

#### 6.6.2 标准曲线管理（原"模块7"，用户口误，已澄清）

| # | 功能点 | 验收标准 |
|---|---|---|
| 6.2.1 | 按指标切换：氨氮 / 硝态氮 / 亚硝态氮 / 总P /（COD 置灰，标注"仪器直读，不用标曲"） |
| 6.2.2 | 标液录入表：浓度 + 吸光度，可加行删行，点数不限 |
| 6.2.3 | 自动拟合：实时算出 k、b、R² |
| 6.2.4 | 拟合散点图：直观显示点位与拟合直线 |
| 6.2.5 | 新建标曲：可设生效日期（默认今天）、试剂批号、备注 |
| 6.2.6 | 生效区间冲突时给出明确提示 |
| 6.2.7 | 历史曲线列表：生效区间、k、b、R²、批号、状态；停用项显示"仍算着 N 条旧数据" |
| 6.2.8 | **新建标曲后，生效日之前的数据浓度一个数字都不变**（最高优先级验收项） |

#### 6.6.3 自定义指标管理

| # | 功能点 |
|---|---|
| 6.3.1 | 自行添加指标：名称、单位、参考范围、默认稀释倍数 |
| 6.3.2 | 可停用指标，停用后不出现在录入界面，历史数据保留 |

#### 6.6.4 备份与导出

| # | 功能点 | 验收标准 |
|---|---|---|
| 6.4.1 | 全量导出 Excel | 所有数据按长表结构导出，含标曲追溯字段 |
| 6.4.2 | 备份文件导出 | 导出一个完整备份文件（含反应器、指标、标曲、全部测量值） |
| 6.4.3 | 备份文件导入 | 可导入备份文件；导入时提示"覆盖 / 合并"并要求确认 |
| 6.4.4 | 报告生成 | 选时间段 + 罐 + 指标，生成带图表的汇总报告，可导出 Excel 或打印为 PDF |

---

## 7. 页面布局与交互约定

### 7.1 整体框架【已确认】

- 左侧 **118px 固定导航栏**，六个模块竖向排列：数据录入 / 全周期 / 查询整理 / 可视化 / 统计分析 / 系统设置
- 右侧为主内容区
- 窄屏（手机）时，侧边导航自动收为**底部图标栏**，录入表格横向可滑动

### 7.2 已确认的页面布局草图

| 页面 | 布局要点 |
|---|---|
| 模块一 日常录入 | 顶部：日期 + 进水模式切换 + 保存<br>中部：进水行 → 空白行 → 稀释行<br>主体：行=罐、列=指标的表格，浓度列绿色只读 |
| 模块二 全周期 | 顶部：周期选择 + 起始/间隔/点数 + Excel 粘贴按钮<br>中部：指标切换 tab<br>主体：行=时间点、列=罐，含阶段标记列<br>底部：四张统计卡片 |
| 模块四 可视化 | 左：筛选面板（数据类型/日期/罐/指标/对比方式）<br>右：标题行 + 导出图片按钮 / 图表 / 图例（带均值）/ 操作提示 |
| 模块六 标曲管理 | 二级 tab → 指标 tab（COD 置灰）<br>当前生效卡片：左=标液表，右=拟合散点图 + k/b/R²/批号四张小卡<br>下方：历史曲线列表 |

### 7.3 通用交互约定

- 需填写的格子：白底 + 细边框
- 自动算出的值：无边框绿色字，只读
- 被单独覆盖的值：橙色角标（如 `×20`）
- 异常值：单元格标黄
- 无数据：显示 `—`，不用 0 填充
- 所有删除操作须二次确认，并告知影响的数据条数
- 所有文案用中文，说人话，不出现技术术语

---

## 8. 非功能需求

### 8.1 数据存储与持久化【已确认，硬性】

- 数据保存在**用户本机**，刷新页面、关闭浏览器、重启电脑后**一条都不能少**
- 不需要登录、不联网、不用云数据库
- 实现方式：浏览器本地数据库（IndexedDB 或同等方案），配合定期提醒用户导出备份

### 8.2 手机端【已确认】

- 形态：**PWA**，手机浏览器打开后"添加到主屏幕"，有图标、全屏、离线可用
- 不做原生 APK
- 电脑与手机**各存一份数据**，通过备份文件导入导出互相搬运

### 8.3 备份与同步

- 首次使用与之后定期，提示用户导出备份
- 备份文件为单一文件，便于微信/QQ 传到手机
- 导入时明确区分"覆盖"与"合并"，默认合并，覆盖需二次确认

### 8.4 性能

- 按当前规模（几罐 × 五年 × 每天几十条 + 周期性密集数据），本地存储完全够用
- 页面切换、图表渲染应在 1 秒内完成

### 8.5 兼容性

- 电脑端：Chrome / Edge 最新版
- 手机端：Android Chrome、iOS Safari（iOS 的 PWA 支持有限，能添加到主屏幕即可）

---

## 9. 第一版范围

### 9.1 做（In Scope）

六个模块的全部功能点，详见第 6 节。

### 9.2 不做（Out of Scope）

| 项 | 原因 |
|---|---|
| 登录、账号、权限 | 单人本机使用 |
| 联网、云同步、多人协作 | 同上 |
| 手机原生安装包（APK） | 用户已选 PWA 方案 |
| 仪器自动读数（串口/蓝牙） | 需额外硬件对接 |
| 动力学模型拟合（Monod 等） | 超出第一版目标 |
| 自动剔除异常值 / 自动修约 | 已确认：只提醒，不自动改 |
| 数据自动报警推送 | 无服务端 |
| 高氯酸盐专属分析模块 | 第一版当普通自定义指标处理，进水浓度按罐记录即可 |
| 多用户、操作审计日志 | 单人使用 |

---

## 10. 已确认假设

以下为替用户做的默认决定，用户已认可：

1. **反应器数量不限**，可随时增删
2. **全周期默认不跨天**，取样间隔默认 30 分钟，均可改
3. **单位统一 mg/L**（自定义指标可自行指定单位）
4. **AOA 阶段标记选填**，不强制
5. **日常录入表按"指标"分行、按"罐"分列**（依据 OQ-1 答复：先测完所有罐的氨氮）
6. **进水同样支持吸光度换算**（进水浓度高时通常也要稀释）

---

## 11. 待定项（Open Questions）

开发前必须解决。

| # | 待定项 | 影响 | 建议 |
|---|---|---|---|
| ~~OQ-1~~ | ✅ 已解决：**先测完所有罐的氨氮**（指标优先、罐次之）。日常录入表采用「按指标分行、按罐分列」 | 已定稿 | 2026-08-30 确认 |
| **OQ-2** | **全周期是否接受一次只录一个指标**（顶部指标切换 tab） | 影响全周期录入页的布局与填写流程 | 若用户希望一屏看全，改为"按罐分组的宽表 + 横向滚动" |
| **OQ-3** | 模块三（查询整理）、模块五（统计分析）的页面布局尚未绘制 | 影响这两个模块的开发 | 沿用现有风格补两张草图 |
| **OQ-4** | **手机端布局**尚未单独绘制 | 影响移动端适配 | 电脑端定稿后补画 |
| **OQ-5** | 制作标准曲线时，标液的吸光度是否已经减过空白？ | 影响 b 的含义与换算公式的严谨性 | 建议：标曲录入时不减空白，统一由软件在测样时减 |
| **OQ-6** | 检出限如何确定（各指标是否固定、是否随标曲变化） | 影响"未检出"的判定 | 建议：在指标设置里手工填一个检出限值，选填 |
| **OQ-7** | 高氯酸盐是作为"进水投加浓度"记，还是也测出水浓度？ | 影响进水表结构与去除率算法 | 用户澄清 |

---

## 附录 A：字段取值枚举

| 字段 | 取值 |
|---|---|
| `Indicator.category` | `basic` 基础指标 / `custom` 自定义 |
| `Indicator.method` | `absorbance` 吸光度换算 / `direct` 直读 |
| `Measurement.scene` | `daily` 日常 / `cycle` 全周期 |
| `Measurement.inputType` | `absorbance` / `direct` |
| `Measurement.phase` | `anaerobic` 厌氧 / `oxic` 好氧 / `anoxic` 缺氧 / 空 |
| `Influent.mode` | `shared` 几罐共用 / `perReactor` 每罐各自 |
| `Reactor.active` | `true` 启用 / `false` 停用 |

## 附录 B：变更记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v1.0 | 2026-08-30 | 首版。基于 5 轮需求澄清与 4 张布局草图评审整理 |
