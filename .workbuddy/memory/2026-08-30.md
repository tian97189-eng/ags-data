# 2026-08-30

## 需求澄清（未写代码，仅确认范围）
用户要求先检查需求缺口、一次问一个问题，再输出第一版范围。

通过 5 轮提问确认了以下关键点（详见 MEMORY.md）：
1. 标曲：每次测定都做多点标准曲线 → 每个指标一条「当前生效」曲线，管一段时间，
   重做不影响历史数据。
2. COD：仪器直接出浓度，手工录入，不走吸光度换算。
3. 进水：两种模式并存 —— 几罐共用 / 每罐各自（高氯酸盐梯度实验）。
4. 部署：电脑与手机各存一份，导入导出同步；手机端 PWA。
5. 公式设置归入「模块六 参数管理」（用户口误说成模块7）。

产出：一张数据流转链路图（标曲 → 吸光度录入 → 自动换算 → 本地存储 → 分析），
以及第一版「做什么 / 暂时不做什么」清单，等待用户确认后才动手。

## 下一步
等用户拍板范围 → 再确定技术栈与建项目。用户明确要求「先不要写代码或创建项目」。

## 追加：页面布局草图（用户已确认范围，要求先看布局）
画了 4 张 HTML 布局草图（全部内联展示，未落文件）：
1. 模块一 日常录入页（同时确立了整体框架：左侧 118px 六模块导航）
2. 模块二 全周期录入页
3. 模块四 可视化对比页（含手绘 SVG 折线图示例）
4. 模块六 标准曲线管理页（含拟合散点图 + 历史曲线列表）

关键设计决策：
- 日常录入把「空白吸光度」和「默认稀释倍数」提到表格上方作为本次共用行，
  避免每格重复填，同时保留单格覆盖能力（橙色 ×20 角标表示被单独改过）
- 全周期用「指标切换 tab」解决 罐×指标 列爆炸问题，一次只录一个指标
- 浓度列一律绿色只读，视觉上区分"要填的"和"算出来的"
- 标曲历史项显示"仍算着 N 条旧数据"，直观传达旧数据不被改写

等待用户对布局的反馈，之后才进入技术选型与建项目。

## 追加：输出 PRD.md
路径 `C:\Users\sky\Desktop\数据APP\PRD.md`（v1.0），作为后续开发依据。
结构：背景目标 → 术语 → 数据模型（8 个实体长表结构）→ 计算规则 → 六模块功能表（含验收标准）
→ 页面布局 → 非功能需求 → In/Out Scope → 已确认假设 → 7 个待定项（OQ-1~7）→ 字段枚举附录。

PRD 里最关键的几条硬约束：
- 浓度 = (A样 − A空 − b) / k × 稀释倍数
- Measurement 冗余存 curveId，旧标曲停用了也能追溯；新建标曲绝不改写生效日之前的数据
- 进水两模式 shared / perReactor；进水缺失时去除率显示"—"，禁止用 0 兜底
- 异常值只标黄提醒，不自动剔除不自动修约
- 存储 IndexedDB，PWA，电脑手机各存一份靠备份文件同步

未解决待定项：OQ-1 填写顺序（罐优先还是指标优先，影响录入表行列方向）、
OQ-2 全周期是否接受单指标切换、OQ-5 标曲吸光度是否已减空白、OQ-7 高氯酸盐记法。

## 追加：输出 DEV_PLAN.md
路径 `C:\Users\sky\Desktop\数据APP\DEV_PLAN.md`（v1.0）。

### 环境实测结果（本机）
node v22.22.2 / npm 10.9.7 / git 2.55.0，无 pnpm，npm 源是官方 registry（已决定切 npmmirror）。

### 技术选型（已定）
React 18 + TypeScript + Vite + Tailwind + Dexie(IndexedDB) + ECharts + SheetJS(xlsx)
+ Zustand + vite-plugin-pwa。包管理用 npm，不用 pnpm。

### 阶段划分 P0~P9
P0 地基 → P1 设置核心(反应器/指标/标曲) → **P2 日常录入(最小可用)** → P3 全周期
→ P4 查询 → P5 可视化 → P6 统计 → P7 备份与报告 → P8 手机端PWA → P9 收尾。
每阶段带可实操的「判据」。P2 完成即可日常使用，后续为增强。

### 数据库设计要点
Dexie 单库 `ags-data`，9 张表。measurements 用长表；冗余存 curveId 供追溯；
defaults 表用 scopeKey 字符串（`daily:2026-08-30` / `cycle:17`）统一日常与周期，
避免 cycleRunId 空值导致的复合索引问题。

### 两个必须提前知道的技术坑
1. PWA 安装必须 HTTPS —— 局域网 http 下 Service Worker 注册不了，手机装不了 PWA。
   对策：自签证书走 https（主选）/ 退化成书签（备选）/ 托管（需联网，与要求冲突）。
2. Windows bat 脚本遇中文路径会乱码 —— start.bat 内容全英文，路径用 `%~dp0` 相对定位。

### 阻塞项
TBD-2（= PRD OQ-1，填写顺序）未答会导致 P2 完成后返工，开工前必须先问。

## 追加：连续开发完成（P0→P9 全交付）
用户答复 OQ-1「先测完所有罐的氨氮」后，按计划连续开发，9 个阶段全部完成。

**成果**
- 完整可用的 AGS 数据台：日常录入（指标卡片式）、全周期（Excel 粘贴）、查询、可视化、统计、设置、备份
- **104 个测试通过**（15 个测试文件），git 存档点 P0~P9 共 10 个 commit
- 交付 `start.bat`（双击启动）和 `README.md`（使用说明）

**过程中遇到并解决的技术坑**（详见 MEMORY.md「踩过的坑」）
1. 沙箱 safe-delete 干扰 npm/vite → 用 dangerouslyDisableSandbox + emptyOutDir:false
2. vitest 16 worker 并发 OOM → 限制 maxThreads=4
3. Dexie 复合索引不能含 null → influents 去掉复合索引改 filter
4. Dexie clear 不重置自增 → 测试用 seed 返回真实 id

**收尾**：PRD/DEV_PLAN 的 OQ-1/TBD-2 已标记「已解决」；其余 OQ（OQ-2/4/5/6/7）
在开发中已按合理默认实现，记录在 MEMORY.md「遗留待定项」。
