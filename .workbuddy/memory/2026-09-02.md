
## 阶段B进阶：图标+概览+深色（08:20-08:40，328测试通过，commit b3f68e1）

按用户截图+三个一起做需求。**回退锚点**：tag `before-advanced-ui`（之前还有 before-visual-v1）。
三件事：①7个导航加线性 SVG 图标 ②新建 /overview 今日概览（4 大数字卡 + 7天趋势 SVG） ③深浅色切换（Sidebar 底部按钮+localStorage）。

关键改动：
- Icons.tsx 自建（避免依赖 lucide），含 Sun/Moon 切换图标
- nav.ts 加 icon 字段，8 项（概览/录入/全周期/查询/其他指标/可视化/统计分析/设置）
- Sidebar 140px（原来118）装图标，BottomNav 加大图标
- /overview 用 useLiveQuery 聚合 measurements+influents+svi+eps+reactors+indicators；
  4 卡：氨氮去除率（今日平均进出水算）、SVI30（最近）、今日样本次数、PN/PS（最近）；
  7天氨氮去除率趋势（每罐一条线，5 段 grid 灰线）
- ErrorBoundary "返回数据录入"→"返回首页"，跳 #/overview
- 深色：Tailwind darkMode:'class' + useAppStore theme（light/dark/system localStorage）+ App useEffect 加 dark class（system 跟随媒体查询）
- 批量 Python 替换给 31 文件关键 class 加 dark: 变体（bg-white→bg-white dark:bg-slate-800 等）
- test/setup.ts mock window.matchMedia（jsdom 没）

**坑**：①useLiveQuery Promise.all 解构 TS 推断不出类型 → 改成返回对象+显式注解；
②jsdom 没 matchMedia → setup mock；③ErrorBoundary.test.tsx Boom 函数返回 never（throw 没 return）→ 加 : JSX.Element 注解。

**回退命令**：
`git reset --hard before-advanced-ui`（三件全撤）或 `before-visual-v1`（撤所有视觉改版）。

## 七连问题（08:56-09:19，全量 44 文件 343 测试通过）

一次解决 7 个问题，每个加测试，最后全量验收。提交分 5 次：
- 22a27f4 fix: 深色模式 dark:text 二次污染清理（220处，text-slate-400/500 互相覆盖）
- 问题1+2：MLSS M2 文字"滤纸+泥+坩埚"→"滤纸+泥"（页面+schema注释）；新增通用 HistoryCalendar 组件（月历高亮+选中日明细），MLSS 历史改日历浏览
- 问题3+4：粒径页 handleSaveRow 修复（允许只填 M1/M2 自动保存，不再因 dry=null 删记录）；新增历史日历（含 d50 汇总）；中位径自动=(from+to)/2（extras.ts 新增 midOfRange 纯函数），有上限段只读、最后一段(>N)手动代表值
- 问题5：他人数据独立空间——otherReactors/otherMeasurements 独立表（Dexie v3）+ /other 页面（罐管理+按指标按罐录入吸光度换算+历史日历），自己的数据物理隔离。otherEntry 测试+5
- 问题6+7：实验记录时间线 + 照片存储——experimentRecords 表（photos 存 base64 DataURL 可进备份）+ /experiment 页面（时间线排版+拍照/选图最多9张+指标多选）。**顺带修复 backup.ts 只备份9张业务表的缺陷**：补齐全部 17 张表 merge/overwrite。

**关键设计决策**：照片存 base64 而非 Blob——因为 backup.ts 用 JSON.stringify，Blob 会变 {}。base64 可序列化、能进备份。

**坑**：Dexie version 链式升级（v3/v4）；schema 测试表数 14→16→17；useLiveQuery 解构类型推断（OtherEntry setCell 里 TS2783 spread 顺序）。

## 三问题修复 round3（12:18-12:27，349测试通过，commit c200cda + 0e06ccc）

打新回退锚点 `before-fixes-round3`。

**问题1：日历bug**——DatePicker 第88行 `<div className="fixed inset-0 z-40">` 全屏蒙层吞滚轮/鼠标点击，且 mobile 盖在 BottomNav 上。改成 mousedown outside-click + ESC 关闭，HistoryCalendar 加 `pb-20 md:pb-0`。电脑端滚轮恢复，手机端不被遮。

**问题2：底部导航4字折行**——10 项 flex-1 平分，4字标签超过 36px宽度被压成2行。
- 标签全部缩到 2-3 字（指标/统计/他人/实验/周期/可视）
- nav 改 `min-w-[60px] flex-shrink-0 overflow-x-auto whitespace-nowrap truncate max-w-[56px]`
- 隐藏滚动条（scrollbar-width:none），保留窄屏横滑能力

**问题3：概览页换内容**——完全脱离实验数据：
- 顶部 hero：青绿渐变 + 实时时钟 HH:MM:SS（每秒）+ 大字日期 + 农历（Intl.DateTimeFormat zh-CN-u-ca-chinese）+ 星期 + 节日标签 + 一句话金句
- 2×2 卡片：今日天气（Open-Meteo 免费无 key，可切换城市）/ 倒计时（事件+日期+天后|天前）/ 今日一言（30 句库按日期稳定选——可能哪天灵感好可以扩充到 100 句）/ 随手记（textarea 防抖 300ms 入 localStorage）
- WMO weather code → emoji+中文映射表；setup.ts 加 fetch mock；Overview 测试 +6

**坑**：Intl.DateTimeFormat 在新版 TS 里已识别不用 `@ts-expect-error`；React.FormEvent<HTMLFormElement> 类型让 `currentTarget.elements` 可识别。

## 实验方法库功能（12:31-12:57，359测试通过）

用户需求：查看实验方法（操作步骤、加药等），配图/PDF 可点开看。多轮确认需求（用户明确要求先策划后开发，给了 6 张实验方法图）。
**已拍板的需求**：入口=其他指标页首 tab「实验方法」；预置 11 个骨架；逐步模式+本步试剂；图文不分离（每步直接配图）；批量导入配图按选择顺序挂未配图步骤；粒径页加跳转按钮。

**实现（commit 90cec3a）**：
- schema v5：methodDocs 表。MethodDoc = name/method/category/scope/reagents[{name,conc,dose,note}]/instruments/steps[{text,image?,reagentRefs}]/warnings/attachments[{name,kind:image|pdf,data:base64}]
- lib/methods.ts：DEFAULT_METHODS 11 个预置（步骤文字已从用户图整理）+ seedMethodsIfEmpty（空库自动写）+ countMedia + compressImage（canvas 1600px/0.82）+ importStepImages（可按注入 compress 便于测试）
- 页面：MethodsTab（列表分类分组卡片）/MethodDetail（5块+完整/逐步切换+本步试剂chip+附件区+Lightbox）/MethodEditor（试剂行/步骤行配图/批量导图/附件上传）
- Lightbox：fixed 全屏 + 滚轮/+-键缩放 50%~400% + ESC 关闭（jsdom 注意 wheel preventDefault 需 passive:false）
- ParticleSizePage 加 onOpenSOP prop → Extras 切 tab 打开粒径方法
- backup.ts 补 methodDocs（merge 按 name 判重防预置重复）

**坑**：countMedia 先返回 attachments:number 被页面当数组用 → 改返回 {stepImages,images,pdfs}；hooks 规则（useMemo 在条件 return 后）→ 抽 MethodListView 子组件。
**测试**：methods +5、Extras 集成 +5、schema 18 表。全量 47 文件 359 测试。回退锚点 before-methods-feature。
**待办**：用户在软件里给 EPS/离子色谱/MLSS 等导入配图（图需从笔记软件导出原图，截图是压缩版）。

## 三个分析功能（13:08-13:14，367测试通过，commit a8ef479）

按用户挑选的"建议清单"实现（用户明确只做 1/2/3，其余不要）：
1. **浓度梯度 vs 去除率图**（Stats 页新增 md:col-span-2 卡）：选 X=浓度/进水指标 + Y=响应指标；每罐每日期一个点（X=该日进水浓度 perReactor 优先否则 shared，Y=去除率）；ECharts scatter 按罐分色 + 整体最小二乘虚线趋势线（红色）+ n/slope/R² 显示。
2. **同罐跨天驯化叠图**（Chart overlay 模式升级）：overlayCycleIds state，checkbox 多选周期列表（可滚动 max-h-40）+ 全选按钮；[] = 全叠兼容原行为。改 lib 层 buildCycleOverlay 传参 → selCycles.map({id,name}) 修 CycleRun 可选 id 类型不匹配。
3. **异常值标红**：outOfRange(value, refLow, refHigh) 纯函数（只判断设置的那侧）；IndicatorCard 浓度列超范围红字 + title 提示参考范围；Query 页超范围整行 bg-red-50/60 + 浓度红字。参考范围 = 指标设置已有的 refLow/refHigh（无需改 schema/设置 UI）。

**关键点**：Indicator 已有 refLow/refHigh 字段（达标率用 refHigh，Stats 处）→ 功能3零 schema 改动。
**坑**：CycleRun[] 传 {id,name}[] 参数 TS 报错（id 可选）→ map 显式 {id:id!,name}；Chart 131/135 ExtrasField 错误为既有 tsc 噪音与本次无关。
**测试**：stats.test +8（outOfRange×4 + linearRegression×4 完美直线/不足2点/x同值/null）。全量 47 文件 367 测试。

## 体验优化五件套（13:18-13:30，375测试通过）

用户"都要"体验优化5项。commit + tag ux-five-features。
A 复制昨天（Entry 顶部按钮，prevDay 复用 format.ts，只复制出水吸光度+稀释override，进水不复制）
B 回收站（schema v6 trashRecords ++id,table,deletedAt；lib/trash.ts trashMeasurements/listTrash/restoreTrash/purgeTrash/emptyTrash/purgeExpiredTrash(30天)；Query 删除改存档+物理删、右上回收站弹窗、恢复写回原表保id；App 启动静默清理）
C 快捷筛选（lib/presets.ts localStorage；Query 筛选卡内预设行：chips 应用/✕删/存为预设内联输名 Enter 保存 ESC 取消）
D 手机录入（保存吸底按钮 bottom-16 在 BottomNav 上、md:hidden、桌面顶部保存隐藏 md:block；IndicatorCard 所有数字输入 onKeyDown Enter → 同卡下一个→下一张卡 data-indicator-card focus+select）
E 标曲天数（IndicatorCard 头部 span：k=… · MM-DD 生效 · 已用N天，curveDays useMemo 自 effectiveFrom，title 悬停）

坑：EntryPage 复制测试必须先 waitFor 输入框值=0.284 再点保存（setCells 异步，同 tick 保存读旧 state→null）；'氨氮'文本多处匹配用 findAllByText。
测试：trash+4/presets+4/IndicatorCard新3/EntryPage+1；schema 19 表。全量 49 文件 375 测试。

## 论文产出五件套（13:34-13:46，389测试通过，tag pub-five-features）

用户"都要"。5 项全部实现：
1. **单日小结**（查询页 action「单日小结」→ DaySummary 全屏视图）：选日期 → 每 active 指标卡（参考范围标注），行=罐：进水/出水/去除率/状态（超范围红字+标记）；顶栏出水计数+超范围数；「今日备注」textarea → lib/dayNotes（settings key dayNote.<date>，进备份，被周报引用）。复用 settings 表未升 schema。
2. **SVG/高清导出**：Chart 页 ReactECharts 加 opts={{renderer:'svg'}}（echarts 5.5 全量含 svg）；「导出 SVG」inst.getDataURL({type:'svg'}) 下载 .svg（期刊矢量）；「导出图片」改高清3x：svg dataURL → Image → canvas scale3 → PNG。
3. **周报**（Stats 页 action「生成周报」→ modal pre + 复制）：lib/weeklyReport.buildWeeklyReport 纯函数——窗口内每罐每指标 进水均值/出水均值/去除率/达标率/超参考次数（1 次超参考标注）+异常明细行+当日备注；lib/recentWindow(7)。
4. **备份提醒**：lib/backupReminder（maybePromptBackup 距上次≥7天且当天未提醒→toast；markBackupDone 设置页导出成功调用；从未备份不打扰）；App 启动 useEffect 检查。
5. **Origin CSV**：lib/csv.buildWideCsv（row=日期升序，col=罐_指标按出现序，值=当日最后一次 daily，UTF-8 BOM）+downloadCsv；Query 页「宽表 CSV」按钮。

坑：周报"超限"按均值判断会漏（均值<上限但单次超）→ 改统计单次超参考次数；csv 单列行尾无逗号断言别加","。
测试 +14（csv3/weekly3+window/dayNotes4/backupReminder4）。全量 53 文件 389。回退锚点 before-round4-pro / pub-five-features。

## 六需求 round（14:24，60文件 418测试通过）
用户清单 6 项（第6性能延后），逐个实现+测试+提交：
1. **录入草稿**：lib/draft.ts（localStorage 单槽+判空只算用户输入不算默认稀释+shouldOfferRestore）；InfluentPanel onStateChange/restoreDraft；Entry 防抖600ms存、当日无DB数据且有草稿弹恢复banner。测试+12。
2. **外观三选一**：设置页加「外观」tab（AppearanceSettings 浅/深/跟随系统，手机端可切）。测试+4。
3. **清空当日进回收站**：trash.ts 泛化 trashRows+restoreTrash 三表(measurements/influents/defaults 保id)；entry.ts deleteDailyDataToTrash；确认弹窗文案改"可恢复"。测试+3。
4. **录入↔方法库互链**：lib/navBus.ts(gotoMethod/consumePendingMethod)；IndicatorCard 指标名旁「方法」→ #/extras 自动打开匹配方法；MethodDetail「去录入」回录入页。测试+4。
5. **超范围原因标注**：DaySummary 超范围可点→行内原因输入→写 measurement.note（已注标记）；weeklyReport 异常记录带"原因：xxx"。测试+6。
6. **性能预留**：未做，等用户确认（数据量小）。
**注意**：今天是9/2，昨天(9/1)已有多轮 APK 打包修复（v5~v8，最终 cmd 原生调 java+日志）——详见 09-01 记录。方法库/回收站/深色主题/概览/单日小结/周报 等都是前两天的成果，六需求是在其上的增量。
